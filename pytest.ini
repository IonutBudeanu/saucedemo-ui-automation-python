[pytest]
addopts = -ra
markers =
    smoke: fast high-value checks
    regression: full regression coverage
    negative: negative / validation tests
    known_bug: expected failures / tracked issues
    compatibility: cross-browser subset (firefox/edge)
